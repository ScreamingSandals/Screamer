class ReactionSnipe {
    constructor(author, emoji, messageUrl, timestamp) {
        this.author = author;
        this.emoji = emoji;
        this.messageUrl = messageUrl;
        this.timestamp = timestamp;
    }
}

module.exports = ReactionSnipe;